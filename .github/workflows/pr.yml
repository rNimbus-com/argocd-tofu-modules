name: ➡️ Pull Request

on:
  - pull_request
  - push

permissions:
  id-token: write
  contents: write
  actions: read
jobs:
  pull-request:
    name: Pull Request
    uses: rNimbus-com/ci-workflows/.github/workflows/pr.yml@feature/tofu-pr
    with:
      working-dir: argocd_app